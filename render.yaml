services:
  - type: web
    name: vertex-fusion
    env: node
    plan: free
    buildCommand: npm install && npx tsx script/build.ts
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
